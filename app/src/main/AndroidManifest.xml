<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pbreakers.pbchat">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- READ/WRITE_EXTERNAL_STORAGE permissions are required to upload or download files from/into external storage. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.messagerie.DiscussionActivity"></activity>
        <activity android:name=".activity.messagerie.DetailDiscussionActivity" /> <!--  -->
        <activity android:name=".activity.messagerie.ContactActivity" /> <!--  -->
        <activity android:name=".activity.login.SignUpActivity" /> <!--  -->
        <activity android:name=".activity.login.LoginActivity" /> <!--  -->
        <activity android:name=".activity.login.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>